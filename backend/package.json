{
  "name": "equilibrio-backend",
  "version": "1.0.0",
  "description": "Backend seguro para Painel do Profissional - EQUILIBRIO",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "test": "jest --testRegex \"tests/.*\\.js$\" --detectOpenHandles",
    "test:watch": "jest --testRegex \"tests/.*\\.js$\" --watch",
    "test:coverage": "jest --testRegex \"tests/.*\\.js$\" --coverage"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^8.5.2",
    "firebase-admin": "^13.10.0",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
